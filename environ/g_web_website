SESSION_PASSWORD dev="testingtestingtestingtestingtestingtestingtestingtestingtestingtestingtestingtestingtestingtesting"
# The password used to encrypt the ironsession cookie.
# It has to be at least 32 characters long. Use https://1password.com/password-generator/ to generate strong passwords.

G_WEB_DATABASE_USER
# The username that is used to connect to the g_web database

G_WEB_DATABASE_PASSWORD
# The password that is used to connect to the g_web database

G_WEB_DATABASE_HOST
# The host that is running the g_web database. This should not include the database name.
# Likely just "localhost".

