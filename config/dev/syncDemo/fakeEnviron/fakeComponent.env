# Fake environ-style var file, used only to prove sync-static-config.py's template substitution
# mechanism in the syncDemo - not a real environ/* file, not read by any real component.
FAKE_VAR=demo-value
