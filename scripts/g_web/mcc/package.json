{
    "name": "@g_web/mcc",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "dotenv -e ../.devenv -- tsx server.ts",
        "build": "tsup server.ts --format cjs --out-dir dist"
    }
}
